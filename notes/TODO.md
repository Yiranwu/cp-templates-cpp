# Algos
Binary lifting template
argrank?
Balanced tree, ref problem: https://codeforces.com/contest/2053/problem/D

SegTree
Prime factor dcmp
Heavy light dcmp
Link cut tree

# Lib improvements
Migrate to C++20, including std::ranges, and use of concepts.
Add more node in segtree
Write some random utilities in datagen to accelerate it.
make graph easier to use
argsort rename
argsort apply argsort
dijkstra and assert no circle in graph
unordered_map of pairs, custom hash function

In C++, if we have unordered_set<int> P[2], how do we write decltype for a variable holding ptr to P[0] or P[1]?

make tree class easier to use: write a function that populates G[tree]

# Workflow

# Agents
Write prompts specifying the style of lib, including ease-of-use features, API doc style.





